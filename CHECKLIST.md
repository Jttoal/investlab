# InvestLab MVP 开发检查清单

## 项目启动前检查

### 环境准备
- [ ] 已安装 JDK 21+
- [ ] 已安装 Node.js 18+
- [ ] 已安装 Gradle 9.0+
- [ ] 已安装 npm 或 yarn

### 依赖安装
- [ ] 后端依赖已下载 (首次运行 `./gradlew bootRun` 会自动下载)
- [ ] 前端依赖已安装 (`cd frontend && npm install`)

## 功能测试清单

### 后端 API 测试

#### 健康检查
- [ ] GET `/api/v1/health` 返回 200
- [ ] GET `/api/v1` 返回欢迎信息

#### 账户管理
- [ ] POST `/api/v1/accounts` 创建账户
- [ ] GET `/api/v1/accounts` 获取账户列表
- [ ] GET `/api/v1/accounts/{id}` 获取账户详情
- [ ] PUT `/api/v1/accounts/{id}` 更新账户
- [ ] DELETE `/api/v1/accounts/{id}` 删除账户

#### 策略管理
- [ ] POST `/api/v1/strategies` 创建策略
- [ ] GET `/api/v1/strategies` 获取策略列表
- [ ] GET `/api/v1/strategies?type=index` 按类型筛选
- [ ] GET `/api/v1/strategies/{id}` 获取策略详情
- [ ] PUT `/api/v1/strategies/{id}` 更新策略
- [ ] DELETE `/api/v1/strategies/{id}` 删除策略

#### 标的管理
- [ ] POST `/api/v1/assets` 创建标的
- [ ] GET `/api/v1/assets?strategyId=1` 获取策略标的
- [ ] PUT `/api/v1/assets/{id}` 更新标的
- [ ] DELETE `/api/v1/assets/{id}` 删除标的

#### 交易管理
- [ ] POST `/api/v1/trades` 创建交易(买入)
- [ ] POST `/api/v1/trades` 创建交易(卖出)
- [ ] POST `/api/v1/trades` 创建交易(分红)
- [ ] GET `/api/v1/trades?strategyId=1` 获取策略交易
- [ ] PUT `/api/v1/trades/{id}` 更新交易
- [ ] DELETE `/api/v1/trades/{id}` 删除交易

#### 观点管理
- [ ] POST `/api/v1/viewpoints` 创建观点
- [ ] GET `/api/v1/viewpoints?strategyId=1` 获取策略观点
- [ ] PUT `/api/v1/viewpoints/{id}` 更新观点
- [ ] DELETE `/api/v1/viewpoints/{id}` 删除观点

#### 仪表盘
- [ ] GET `/api/v1/dashboard` 获取仪表盘数据
- [ ] GET `/api/v1/dashboard/strategies/{id}` 获取策略详情

### 前端功能测试

#### 页面访问
- [ ] 访问 `http://localhost:5173` 正常显示
- [ ] 导航栏显示正常
- [ ] 路由切换正常

#### 仪表盘页面
- [ ] 显示账户汇总
- [ ] 显示策略汇总
- [ ] 显示收益统计
- [ ] 点击"查看详情"跳转到策略详情

#### 账户管理页面
- [ ] 显示账户列表
- [ ] 点击"新建账户"打开模态框
- [ ] 创建账户成功
- [ ] 编辑账户成功
- [ ] 删除账户成功

#### 策略管理页面
- [ ] 显示策略列表(卡片形式)
- [ ] 按类型筛选策略
- [ ] 点击"新建策略"打开模态框
- [ ] 创建策略成功
- [ ] 编辑策略成功
- [ ] 删除策略成功
- [ ] 点击策略卡片跳转到详情页

#### 策略详情页面
- [ ] 显示策略基本信息
- [ ] 显示策略汇总数据
- [ ] 标签页切换正常

##### 持仓标的标签页
- [ ] 显示标的列表
- [ ] 显示持仓和收益信息
- [ ] 显示价格提醒状态
- [ ] 点击"添加标的"打开模态框
- [ ] 创建标的成功
- [ ] 编辑标的成功

##### 交易记录标签页
- [ ] 显示交易列表
- [ ] 点击"记录交易"打开模态框
- [ ] 创建买入交易成功
- [ ] 创建卖出交易成功
- [ ] 创建分红记录成功

##### 大V观点标签页
- [ ] 显示观点列表
- [ ] 点击"添加观点"打开模态框
- [ ] 创建观点成功
- [ ] 点击"查看原文"打开外部链接

### 业务逻辑测试

#### 持仓计算
- [ ] 买入后持仓增加
- [ ] 卖出后持仓减少
- [ ] 多次买入成本计算正确
- [ ] 分红后成本减少

#### 收益计算
- [ ] 盈利显示为绿色正数
- [ ] 亏损显示为红色负数
- [ ] 盈亏率计算正确

#### 价格提醒
- [ ] 价格低于目标低价显示"低于目标价"
- [ ] 价格高于目标高价显示"高于目标价"
- [ ] 价格在区间内显示"正常"

### 错误处理测试

#### 后端错误
- [ ] 访问不存在的资源返回404
- [ ] 创建重复资源返回错误
- [ ] 删除不存在的资源返回404
- [ ] 数据验证失败返回400

#### 前端错误
- [ ] API 错误弹出提示
- [ ] 表单验证正常
- [ ] 必填项提示正常

### 数据库测试

#### 数据持久化
- [ ] 创建的数据重启后仍存在
- [ ] 更新的数据正确保存
- [ ] 删除的数据不再显示

#### 级联删除
- [ ] 删除策略时关联的标的被删除
- [ ] 删除策略时关联的交易被删除
- [ ] 删除策略时关联的观点被删除

## 性能测试

### 响应时间
- [ ] API 响应时间 < 300ms
- [ ] 页面首屏加载 < 2.5s
- [ ] 路由切换流畅

### 资源占用
- [ ] 前端构建大小 < 2MB (gzip)
- [ ] 后端内存占用合理
- [ ] 数据库文件大小合理

## 代码质量检查

### 后端
- [ ] 代码符合 Kotlin 规范
- [ ] 没有明显的代码重复
- [ ] 异常处理完善
- [ ] 日志记录合理

### 前端
- [ ] 代码符合 ESLint 规范
- [ ] 组件命名规范
- [ ] 样式使用 scoped
- [ ] 没有 console.error

## 文档检查

- [ ] README.md 完整
- [ ] QUICKSTART.md 可用
- [ ] API 文档准确
- [ ] 开发规范清晰

## 部署准备

### 后端
- [ ] 可以成功构建 jar 包
- [ ] jar 包可以独立运行
- [ ] 数据库文件路径正确

### 前端
- [ ] 可以成功构建生产版本
- [ ] 构建产物可以正常访问
- [ ] API 地址配置正确

## 安全检查

- [ ] 没有硬编码的密钥
- [ ] .gitignore 配置正确
- [ ] 数据库文件不在版本控制中
- [ ] 敏感信息使用环境变量

## 最终检查

- [ ] 所有 TODO 已完成
- [ ] 所有测试通过
- [ ] 文档齐全
- [ ] 代码已提交
- [ ] 准备好演示

---

**检查日期**: ___________
**检查人**: ___________
**状态**: [ ] 通过 [ ] 需要修复
